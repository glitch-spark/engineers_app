# engineers_app

Vite + React 18 + Tailwind SPA. Cursor rules: `.cursor/rules/`.

- Push/PRs: `spark` → `glitch-spark/engineers_app`. Do not push to `bcano-office`.
- Not Next.js. SWR + `src/api/endpoints.ts`. App chrome is `Topbar` + `Sidebar`.
